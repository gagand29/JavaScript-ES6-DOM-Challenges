var a = "3";
var b = "2";

// Swap the values of variables a and b
var temp = a; // Store the value of a temporarily
a = b;        // Assign the value of b to a
b = temp;     // Assign the stored value of a to b

// Output the values of a and b
console.log("a is", a);
console.log("b is", b);
